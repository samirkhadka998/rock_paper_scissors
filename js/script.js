function getComputerChoice() {
    let gameValues = ['Rock', 'Paper', 'Scissor'];

    let randomNumber = Math.floor(Math.random()*3);

    console.log(gameValues[randomNumber])

    // return gameValues[randomNumber];
}